name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - run: npm ci || npm install
      - run: npm run build

      - name: Copy web build into Android assets
        run: |
          rm -rf android/app/src/main/assets/www
          mkdir -p android/app/src/main/assets/www
          cp -a dist/. android/app/src/main/assets/www/

      - name: Stamp rising versionCode
        run: |
          VC=${{ github.run_number }}
          sed -i "s/versionCode [0-9]*/versionCode ${VC}/" android/app/build.gradle
          sed -i "s/versionName \"[^\"]*\"/versionName \"1.${VC}.0\"/" android/app/build.gradle
          grep version android/app/build.gradle

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build debug APK
        working-directory: android
        run: gradle assembleDebug --no-daemon

      - uses: actions/upload-artifact@v4
        with:
          name: kysmindset-apk
          path: android/app/build/outputs/apk/debug/*.apk

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: apk-latest
          name: Kysmindset APK
          body: |
            versionCode: ${{ github.run_number }}
            Real Kysmindset app (Vite build inside Android WebView).
            PIN 1234.
            Debug APK — Play Protect may warn.
            In-app update: Config → App update.
          files: android/app/build/outputs/apk/debug/app-debug.apk
          make_latest: true
